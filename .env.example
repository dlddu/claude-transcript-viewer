# Backend Configuration
PORT=3000
NODE_ENV=production

# AWS S3 Configuration
TRANSCRIPT_BUCKET=your-bucket-name
AWS_REGION=ap-northeast-2

# Note: AWS credentials are not needed when using EC2 Instance Profile
# The application will automatically use the instance's IAM role

# Frontend Configuration (for local development only)
VITE_API_URL=http://localhost:3000
